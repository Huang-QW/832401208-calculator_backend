{
  "name": "calculator-backend",
  "version": "1.0.0",
  "private": true,
  "description": "前后端分离计算器系统 —— 后端服务（表达式解析、计算、历史记录持久化）",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test tests/"
  },
  "engines": {
    "node": ">=24.0.0"
  },
  "keywords": [
    "calculator",
    "express",
    "sqlite",
    "expression-parser"
  ],
  "license": "MIT",
  "dependencies": {
    "express": "^4.21.2"
  }
}
